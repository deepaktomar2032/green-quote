# Base URL of the backend API (include the /api path prefix)
# In local development this points to the NestJS server running on port 3000
# In production set this to your deployed API domain, e.g. https://api.example.com/api
VITE_API_BASE_URL=http://localhost:3000/api
